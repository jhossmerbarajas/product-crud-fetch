# Product Crud PHP - Fetch

Este ejemplo muestra la creación un crud sin regcargar la página usando lenguajes de programación ```PHP``` como **Backend** y ```Javascript``` para el **Frontend**; además 
usando la ```API de Fetch.``` 
